import type { FormFieldType } from '../../types';

export const paragraphConfig: FormFieldType = {
  fieldId: 'paragraph',
  fieldType: 'paragraph',
  name: 'paragraph',
  label: 'Paragraph',
  align: 'center',
};
