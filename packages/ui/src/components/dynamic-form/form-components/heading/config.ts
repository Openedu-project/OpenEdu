import type { FormFieldType } from '../../types';

export const headingConfig: FormFieldType = {
  fieldType: 'heading',
  name: 'heading',
  label: 'Heading',
  headingType: 'h3',
  align: 'center',
};
