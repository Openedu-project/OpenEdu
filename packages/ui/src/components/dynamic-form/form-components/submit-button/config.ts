import type { FormFieldType } from '../../types';

export const submitButtonDefaultConfig: FormFieldType = {
  fieldId: 'submit-button',
  fieldType: 'submitButton',
  name: 'submit-button',
  label: 'Submit Button',
  align: 'end',
};
